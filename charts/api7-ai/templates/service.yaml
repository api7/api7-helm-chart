apiVersion: v1
kind: Service
metadata:
  name: {{ include "api7-ai.fullname" . }}
  labels:
    {{- include "api7-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api7-ai.selectorLabels" . | nindent 4 }}
