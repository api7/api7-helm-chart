apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aisix-cp.fullname" . }}-ui
  labels:
    {{- include "aisix-cp.labels" . | nindent 4 }}
    {{- include "aisix-cp.selectorLabels" (dict "root" . "component" "ui") | nindent 4 }}
spec:
  replicas: {{ .Values.ui.replicaCount }}
  selector:
    matchLabels:
      {{- include "aisix-cp.selectorLabels" (dict "root" . "component" "ui") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aisix-cp.labels" . | nindent 8 }}
        {{- include "aisix-cp.selectorLabels" (dict "root" . "component" "ui") | nindent 8 }}
    spec:
      {{- include "aisix-cp.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "aisix-cp.serviceAccountName" . }}
      {{- with .Values.ui.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: ui
          image: "{{ .Values.ui.image.repository }}:{{ .Values.ui.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.ui.image.pullPolicy }}
          {{- with .Values.ui.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "aisix-cp.pgSecretName" . }}
                  key: {{ include "aisix-cp.pgPasswordSecretKey" . }}
            - name: BETTER_AUTH_URL
              value: {{ .Values.api.publicBaseURL | quote }}
            - name: BETTER_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "aisix-cp.secretName" . }}
                  key: better-auth-secret
            - name: AISIX_CLOUD_DATABASE_URL
              value: {{ include "aisix-cp.databaseURL" . }}
            - name: AISIX_DASHBOARD_LOCALE
              value: {{ .Values.ui.defaultLocale | quote }}
            {{- with .Values.ui.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          {{- with .Values.ui.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.ui.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
